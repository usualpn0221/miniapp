

  <nav>
    <div class="nav-wrapper teal lighten-2">
      <a href="#" class="brand-logo">ブログ</a>
      <% if user_signed_in? %>
        <span id="hello">こんにちは<%=current_user.name%>さん</span>
        <% else %>
        <span id="hello">こんにちはゲストさん</span>
      <% end %>
      <ul id="nav-mobile" class="right hide-on-med-and-down">
        <% if user_signed_in? %>
            <div class="user_nav grid-6">
              <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></li>
              <li><a class="post" href="/blogs/new">新規作成する</a></li>
              <li><%= link_to "マイページ", "/users/#{current_user.id}", class: 'get' %></li>
            </div>
          <% else %>
            <div class="grid-6">
              <li><%= link_to "ログイン", new_user_session_path, class: 'post' %></li>
              <li><%= link_to "新規登録", new_user_registration_path, class: 'post' %></li>
            </div>
          <% end %>
      </ul>
    </div>
  </nav>

 <% @blogs.each do |blog| %>

<%= render partial: "blog", locals: { blog: blog } %>
  <% end %>

      <div class="row">
        <div class="col s12 m6">
          <div class="card blue-grey darken-1">
            <div class="card-content white-text">
              <span class="card-title">Card Title</span>
              <p>I am a very simple card. I am good at containing small bits of information.
              I am convenient because I require little markup to use effectively.</p>
            </div>
            <div class="card-tabs">
              <ul class="tabs tabs-fixed-width">
                <li class="tab"><a href="#test4">詳細</a></li>
                <li class="tab"><a href="#test5">更新</a></li>
                <li class="tab"><a href="#test6">削除</a></li>
              </ul>
            </div>
          </div>
        </div>
      </div>



<script type="text/javascript" src="./app.js"></script>


